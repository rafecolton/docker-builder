version = 1

[docker]

build_opts = ["--rm", "--no-cache"]
tag_opts = ["--force"]

[[container]]
name = "base"
Dockerfile = "Dockerfile.base"
registry = "rafecolton"
project = "docker-builder-base"
tags = ["latest"]

# vim:ft=toml
